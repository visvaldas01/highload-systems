server:
  port: 8084

spring:
  application:
    name: scrobble

  r2dbc:
    password: kt^#54hfff@%*t53gfhj
    username: user-db
    url: r2dbc:postgresql://postgres:5432/itmusik

#  datasource:
#    username: user-db
#    password: kt^#54hfff@%*t53gfhj
#    url: jdbc:postgresql://localhost:5432/itmusik
#    driver-class-name: org.postgresql.Driver

  main:
    allow-circular-references: true

  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

  config:
    import:
      optional:configserver:http://config-server:8888

springdoc:
  api-docs:
    path: /scrobbles/v3/api-docs

  swagger-ui:
    path: /scrobbles/swagger-ui.html

  endpoints:
    web:
      exposure:
        include: /*

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://eureka-server:8761/eureka}